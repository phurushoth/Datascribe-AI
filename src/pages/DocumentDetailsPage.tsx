import { useEffect, useState } from 'react';
import { Link, useParams } from 'react-router-dom';
import type { DocumentRecord } from '../types/app';
export default function DocumentDetailsPage(){const {id}=useParams();const [doc,setDoc]=useState<DocumentRecord|null>(null);useEffect(()=>{fetch(`/api/documents/${id}`,{credentials:'include'}).then(r=>r.ok?r.json():null).then(d=>setDoc(d?.document||null))},[id]);if(!doc)return <div className="p-8 text-slate-500">Loading document...</div>;return <div className="mx-auto max-w-5xl space-y-6 p-5 sm:p-8"><Link to="/documents" className="text-sm font-semibold text-indigo-600">← Documents</Link><div className="rounded-3xl border border-slate-200 bg-white p-7 shadow-sm"><p className="text-sm font-semibold text-indigo-600">{doc.documentType}</p><h1 className="mt-2 text-3xl font-extrabold">{doc.title}</h1><p className="mt-2 text-sm text-slate-400">Created {new Date(doc.createdAt).toLocaleString()}</p><div className="mt-8 grid gap-4 sm:grid-cols-3"><div><p className="text-xs uppercase tracking-wider text-slate-400">Workflow</p><p className="mt-1 font-semibold capitalize">{doc.workflow}</p></div><div><p className="text-xs uppercase tracking-wider text-slate-400">Status</p><p className="mt-1 font-semibold capitalize text-emerald-600">{doc.status}</p></div><div><p className="text-xs uppercase tracking-wider text-slate-400">Drive file</p><p className="mt-1 font-semibold">{doc.driveFileId ? 'Connected' : 'Not connected yet'}</p></div></div></div></div>}
